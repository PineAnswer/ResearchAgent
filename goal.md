# ResearchAgent P0：文献库 Agent 化

## 当前状态

**三个 P0 已于 2026-07-17 完成实现并通过全量回归测试。**

| 优先级 | 功能 | 状态 | 可验收结果 |
| --- | --- | --- | --- |
| P0 | AI 精读入库 | 已完成 | 上传 PDF 后自动分页、分块、索引并生成库级 PaperCard；结论只保留能在对应页找到的原文引文 |
| P0 | 文献库优先研究 | 已完成 | literature-scout 被程序强制先查本地库；paper-reader 对库内论文优先复用全文索引和历史证据 |
| P0 | Ask Library Agent | 已完成 | 无需手选论文，可对全库执行多轮工具检索；回答仅接受本轮真实来源 ID，并展示页码和原文 |

## P0-1：AI 精读入库

### 实现

- PDF 上传后依次进入 `uploaded → extracting → indexed`，失败进入 `failed`，不再把“文件已保存”误报为“全文可用”。
- 使用 `pypdf` 按真实页码提取文本，最多解析 100 页；按页切成带重叠的稳定文本块。
- SQLite 新增 `library_chunks` 和 `library_artifacts`，保存页码、附件、内容哈希、分块序号和版本化精读产物。
- AI 精读结构包含 `summary`、`methods`、`datasets`、`findings`、`limitations` 和 `keywords`。
- 每条 finding 必须带原文 `quote` 和 `page`。程序会再次校验：规范化空白后，引用必须真实出现在该页；不匹配、无页码或空引用会被丢弃。
- 文献详情页新增“AI 精读卡”，展示方法、数据集、局限、结论、页码和原文；附件状态展示页数、分块数、错误原因及“重试解析”。
- 重复解析会替换该附件的全文索引，同时保留版本化精读卡，便于审核历史结果。

### 降级与边界

- 模型不可用时仍完成本地分页和全文索引，并保存 `extractive` 精读卡；不会伪造方法或结论。
- 扫描版 PDF 当前没有 OCR。无法提取文本时状态为 `failed`，原文件仍可下载并可重试。
- 单文件上传上限保持 30 MB；页数上限为 100 页，防止一次请求无界消耗资源。

## P0-2：文献库优先研究

### 实现

- literature-scout 新增 `search_library`，统一搜索论文元数据、摘要、笔记、AI 精读卡、PDF 全文分块和历史项目 PaperCard 证据。
- `ExecutedSearchTrackingMiddleware` 会在程序层阻止尚未执行本地检索的 OpenAlex/Crossref 调用，不能只靠提示词绕过。
- 本地检索完成后，Agent 只能针对明确的 `coverage_gaps` 改写外部查询；与本地查询重复的检索意图仍会被去重。
- 候选论文新增 `library_id`。库内候选会把该 ID 一直传到筛选上下文和 paper-reader；外部候选保持空值。
- paper-reader 遇到 `library_id` 时优先调用 `retrieve_library_passages`，复用带页码全文、AI 精读、笔记和历史证据，不再重复下载和阅读同一论文。
- 本地与外部结果继续进入同一 SearchReport、人工检索审核和后续证据状态机，不另建旁路流程。

### 成本与质量保证

- 本地库已覆盖的问题可以不调用 OpenAlex/Crossref，降低检索费用和重复阅读。
- 本地库覆盖不足时仍允许联网补缺，不会为了省调用而隐藏证据空白。
- 本地来源和外部来源共享真实 paper_id/DOI 去重逻辑，并保留 `source=library` 供前端辨识。

## P0-3：真正的 Ask Library Agent

### 实现

- 文献库工具栏新增“询问文献库”，不选择论文即可检索整个文献库；“对照阅读”仍可把范围限制在选中的 2–8 篇。
- Ask Agent 可串行调用 `search_library`、`retrieve_library_passages` 和 `get_library_paper_context`，完成问题拆解、候选定位、追加检索和证据核对。
- 每次运行都有独立 `source_registry`。模型只能提交本轮工具实际返回的 `source_id`，不能把标题、DOI、library_id 或虚构编号当作引用。
- 回答中的事实行必须包含 `[[source_id]]`；后端验证后再转换为 `[1]`、`[2]`，并返回标题、页码、来源类型和原文摘录。
- 出现未登记来源、无有效引用、未逐行标注来源或空回答时，结果不会作为 Agent 答案返回，而是安全降级为本地相关原文列表。
- 前端使用 Markdown 渲染回答，单独展示覆盖说明和可点击引用来源；点击来源可回到对应文献详情。

### 降级行为

- 模型、网络或结构化输出不可用时，Ask Library 自动返回本地检索最相关的摘要、全文、笔记或历史证据，并明确标记 `mode=extractive`。
- 文献库没有可用材料时明确说明证据不足，不调用外部网络替用户补写答案。

## 使用路径

1. **AI 精读**：打开“文献库” → 选择论文 → “上传 PDF”。等待附件显示“已索引”，随后查看“AI 精读卡”。失败时悬停状态查看原因并点击“重试解析”。
2. **本地优先研究**：直接发起新研究。检索 Agent 会先查本地库；在候选审核中，库内论文显示 `source=library`，确认后继续即可复用全文索引。
3. **全库提问**：打开“文献库” → “询问文献库” → 输入问题 → “开始检索”。不需要勾选论文。
4. **限定范围提问**：勾选 2–8 篇文献 → “对照阅读” → 在对照面板提问，Agent 只可访问选中范围。

## 数据与接口

- 新增库级模型：`LibraryChunk`、`LibraryFinding`、`LibraryPaperAnalysis`、`LibraryArtifact`、`LibraryAgentResponse`。
- 新增接口：`POST /api/library/attachments/{attachment_id}/ingest`。
- `POST /api/library/assistant` 的 `library_ids` 现在可为空；空列表表示全库，最多可限定 50 篇。
- 上传接口会在保存文件后自动执行解析和索引；附件内容接口在解析失败时仍允许下载原文件。
- 数据库启动时自动创建新增表；旧附件和旧文献记录通过 Pydantic 默认字段向后兼容。

## 验收结果

- [x] 全量 Python 测试：`121 passed`。
- [x] Ruff：`ruff check --no-cache src tests` 通过。
- [x] 前端 JavaScript：Node `--check` 通过。
- [x] `git diff --check` 通过，无补丁空白错误。
- [x] 未启动用户服务，未改动用户现有运行数据库，未创建新的 Conda 环境。
- [x] 采用独立功能分支和 Pull Request 发布，不直接修改远端 `main`。

---

# 历史目标：文献研究工作台桌面端视觉重构

## 目标

将现有原生 HTML、CSS、JavaScript 前端完整重构为 `Apple Pages x Nature`
风格的专业学术编辑工作台。新版采用冷白、墨色和松绿，强调成果阅读、
编辑式留白与克制交互，并用真实图标替代字符图标。

保留全部研究功能、后端 API、数据模型和研究阶段，不改变现有业务流程。

## 设计方向

- 桌面网页专用，最低支持 `1280x720`，Windows、Chrome 和 Edge 优先。
- 保留 `Evidence Research Agent / 文献研究工作台` 中英双语品牌。
- 使用舒展但不浪费空间的编辑式留白，核心成果和当前操作尽量首屏可见。
- 页面以最终研究成果为中心；未完成项目根据阶段显示当前最重要任务。
- 不使用渐变、装饰光斑、营销式 Hero、巨大空区或夸张动效。
- 不实现手机和平板布局、暗色模式、营销首页或后端功能变更。

## 视觉系统

- 建立统一的 4/8px 间距尺度和固定字号层级，所有字距设为 `0`。
- 控件高度统一为 32px 或 40px，卡片和面板圆角不超过 8px。
- 色板固定为冷白 `#F6F7F5`、纸白 `#FFFFFF`、墨色 `#191C1A`、
  中性灰和松绿 `#245B48`。
- 红色仅用于危险操作，警告色仅在必须区分状态时出现。
- 阴影只用于抽屉、菜单和浮层，不用阴影堆叠页面层级。
- 本地打包 Inter 作为界面字体，Noto Serif CJK SC 作为标题和研究成果字体，
  并保留对应开源许可证。
- 本地打包 Lucide 浏览器图标库，替换字符图标。
- 所有图标按钮使用稳定尺寸，并提供 tooltip、`aria-label` 和键盘焦点样式。

## 布局与导航

- 顶部改为 64px 高的编辑部刊头，品牌位于左侧，服务状态和工具菜单位于右侧。
- 项目侧栏在宽屏默认展开为 280px，在 `1280-1439px` 默认收窄为 72px。
- 用户可以手动展开或收窄侧栏，状态保存到
  `research-agent.sidebar-state`，不保存项目数据或敏感信息。
- 收窄侧栏保留新建研究、项目库和当前项目状态的图标入口。
- API 文档、按项目 ID 打开和其他低频工具移入“更多”菜单。
- 删除项目移入项目操作菜单，继续保留不可撤销的二次确认。

## 核心工作流

### 新建研究

- “发起新研究”在主工作区打开研究简报，不再把长表单塞进侧栏。
- 主题和研究问题作为主要字段，论文数量和检索轮次作为次级设置。
- 精简说明文字并统一术语，不改变字段含义或提交格式。

### 项目详情

- 已完成项目首先展示最终综述、摘要、章节、引用与参考文献。
- 未完成项目根据阶段显示检索进度、候选审核或继续研究操作。
- 将当前阶段和关键数量整理为安静的元数据条，减少指标卡片堆叠。
- 使用窄阅读列保证长篇中文综述的行宽、段落间距和引用可读性。

### 候选论文审核

- 将论文卡片改为编辑式列表，统一勾选区、标题、作者、来源、年份和摘要层级。
- 筛选工具栏保持可见，候选数量与选中数量使用紧凑的内联统计。
- 将补充检索和反馈参数组织成一个连贯表单，不使用卡片嵌套。
- 保留全部保留、全部取消、更新候选集、确认选择和证据不足结束项目功能。

### 成果与高级信息

- 主区仅保留成果内容和当前任务，研究过程与原始产物移入右侧检查器。
- 检查器宽度为 420px，提供“过程 / 产物”标签和“可读 / 原始”切换。
- 检查器支持关闭按钮、`Esc` 关闭、键盘焦点管理和轻微过渡动画。
- 现有所有产物渲染器、原始 JSON 和时间线数据继续可访问。

## 实现约束

- 主要修改集中在 `src/research_agent/api/frontend/` 下的 HTML、CSS 和 JavaScript。
- 后端接口、请求结构、数据库和研究 Agent 行为保持不变。
- 静态资源新增 Lucide 浏览器包、Inter、Noto Serif CJK SC 及许可证文件。
- DOM 结构和 JavaScript 渲染逻辑必须同步更新，不留下失效选择器或事件监听器。
- 动效只用于侧栏、抽屉、菜单、按钮反馈和状态切换。
- 完整支持 `prefers-reduced-motion`，禁用后不影响任何操作。

## 测试计划

- 保持现有 Python 测试全部通过，并补充字体、图标和静态资源可访问性测试。
- 对 `app.js` 执行 JavaScript 语法检查。
- 扫描前端，确保按钮和装饰区域不再使用 emoji 或字符图标。
- 使用独立临时端口和临时数据目录启动测试服务，不接触用户现有研究数据库。
- 使用 Playwright 覆盖空状态、新建、运行中、候选审核、完成成果、检查器、
  侧栏展开和侧栏收窄状态。
- 截图验收尺寸为 `1280x720`、`1440x900` 和 `1920x1080`。
- 检查文字溢出、组件重叠、横向滚动、间距一致性、首屏层级和长文阅读效果。
- 在 Chrome 和 Edge 完成交互冒烟测试，包括键盘导航、焦点、tooltip、菜单、
  抽屉和删除确认。
- 临时测试服务在视觉验收完成后立即关闭。

## 验收标准

- 所有现有研究功能和 API 行为保持可用。
- 全部前端状态使用同一套颜色、字体、图标、间距和控件规范。
- 页面中不存在用于操作或装饰的 emoji、字符图标、渐变和装饰性光斑。
- `1280x720` 下核心任务可操作，`1440x900` 和 `1920x1080` 下布局舒展但不松散。
- 最终成果是项目页的视觉中心，流程和原始数据不会干扰阅读。
- 无文本截断、无控件重叠、无意外横向滚动、无卡片嵌套。
- 自动化测试、JavaScript 检查与 Playwright 视觉验收全部通过。

## 发布假设

- 本任务只完成前端设计与实现，不修改后端能力。
- 实现完成后不自动提交或推送 GitHub，除非用户另行要求。

## 实施结果

- [x] 桌面端壳层、64px 刊头、可记忆折叠侧栏和主工作区研究简报已完成。
- [x] 最终成果优先阅读、阶段任务、候选审核和 420px 研究记录检查器已完成。
- [x] Lucide、Inter、Noto Serif CJK SC 与对应许可证已本地打包并进入 wheel。
- [x] 字符图标、emoji、渐变、装饰光斑和卡片嵌套已从前端操作界面移除。
- [x] `prefers-reduced-motion`、键盘焦点循环、`Esc` 关闭和删除二次确认已验证。
- [x] Python 测试 `91 passed`，Ruff 与 `app.js` 语法检查通过。
- [x] Playwright 已覆盖 Chrome、Edge、三档桌面尺寸和主要研究状态；无横向溢出、
  控件裁切、渐变残留、字体失败或图标占位残留。
- [x] 临时测试服务、浏览器进程和临时数据目录均已关闭或清理。
- [x] 未提交、未推送 GitHub，后端 API、数据库和 Agent 行为未修改。
